import {Component} from 'angular2/core';

const template: string = require('./footer.html');
const styles: string = require('./footer.scss');

@Component({
	selector: 'footer',
	styles: [styles],
	template
})

export class FooterComponent {}
